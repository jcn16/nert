rendering is the original one

rendring_part is for corase-fine render to train nerf

rendering_nert is for NERT ,including corase-fine render and visibility module

render_visibility is to test visibility module, only include fine sigma and visibility, sigma is pretained
